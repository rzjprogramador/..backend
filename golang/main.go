package main

import "fmt"

func main() {
	// 
	// publicar := "{ } # $ % @ ! & * ^ (  ) [ ] + × ÷ = / _ € £ ¥ ₩ , : ; ? ."
	fmt.Println("{ } # $ % @ ! & * ^ (  ) [ ] + × ÷ = / _ € £ ¥ ₩ , : ; ? .")
}